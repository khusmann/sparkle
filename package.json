{
  "name": "sparkle-runtime",
  "version": "0.1.0",
  "description": "JavaScript runtime for Sparkle R package",
  "private": true,
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
